- [x] 新单词的答对进度（选择题/填空题）能够被 `AppContext` 记录。
- [x] 会话结束时的 `SessionSummary` 不再仅显示“掌握”的数量，而是显示所有取得进展的单词。
- [x] 每个取得进展的单词旁能正确显示其当前的阶段（如：🌱 初识、🌿 熟悉、🌳 掌握）。
- [x] 现有的 4 阶段 SRS（间隔重复）的核心后台逻辑（如升降级、下一次复习时间的计算）未被破坏。
