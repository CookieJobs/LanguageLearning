# 任务列表

- [x] 任务 1: 建立设计系统基础
  - [x] 更新 `tailwind.config.js`，包含所有 `duo` 颜色（含边框色阶）。
  - [x] 在配置中定义标准边框宽度、圆角和间距。
  - [x] 清理 `index.css`：移除 `.glass`、`.glass-card` 和其他未使用的工具类。为 `body` 添加基础样式（如 `bg-gray-50`）。

- [x] 任务 2: 重构核心组件
  - [x] **按钮 (Button)**: 完成 `Button` 组件，严格使用新变体。移除旧的 `primary`/`secondary` 渐变样式。
  - [x] **输入框 (Inputs)**: 在 `Auth.tsx` 中创建/重构输入字段（或提取为 `Input.tsx`）以匹配新样式（灰色背景，实心聚焦）。
  - [x] **卡片 (Cards)**: 标准化卡片样式（白色背景，rounded-2xl，border-2，border-b-4）。

- [x] 任务 3: 更新全局布局
  - [x] **页眉 (Header)**: 重构 `Header.tsx` 使用实心颜色、更新后的 `Logo` 和新的 `Button` 变体（用于“登录”/“退出”）。
  - [x] **布局包装器**: 确保所有页面具有正确的背景颜色（移除全局渐变）。

- [x] 任务 4: 迁移页面 - 认证
  - [x] **登录页 / Auth 组件**: 重新设计登录表单容器。移除背景斑点。使用居中的干净卡片布局。
  - [x] **注册流程**: 对注册视图应用相同样式。

- [x] 任务 5: 迁移页面 - 仪表盘与导航
  - [x] **首页 (HomePage)**: 重新设计“关卡/课程”卡片。使用 3D 卡片样式。
  - [x] **关卡选择页 (LevelSelectPage)**: 更新关卡选择卡片，使其大而醒目且可按压。
  - [x] **复习页 (ReviewPage)**: 将复习列表项更新为可按压的行/卡片。

- [x] 任务 6: 迁移页面 - 学习与反馈
  - [x] **学习总结 (SessionSummary)**: 重新设计结果屏幕。统计数据使用粗体排版。“继续”按钮应突出显示。
  - [x] **模态框**: 更新 `StoryModal`、`SessionExpiredModal` 使用新的卡片样式和关闭按钮。

- [x] 任务 7: 最终润色与验证
  - [x] **动效**: 为所有新卡片添加入场动画（`fade-in-up`）。
  - [x] **响应式检查**: 验证所有新布局在移动端的表现。
  - [x] **无障碍性**: 检查新按钮/卡片上的文本对比度。
