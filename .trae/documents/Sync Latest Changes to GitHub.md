I noticed there are numerous changes in the workspace, including:
- Modifications to existing frontend and backend files.
- New untracked files such as `README.md` documentation in various directories.
- New components and services like `SessionExpiredModal.tsx` and `apiClient.ts`.
- Configuration files for Husky and other tools.

I will proceed with the following steps to sync these changes to GitHub:

1.  **Stage All Changes**: Run `git add .` to include all modified and new files.
2.  **Commit Changes**: Create a commit with the message "Update: Sync latest changes including new components and documentation".
3.  **Push to GitHub**: Upload the changes to the `main` branch of the remote repository.

Please confirm if you want me to proceed with these updates.