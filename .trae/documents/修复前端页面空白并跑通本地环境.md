## 问题初步诊断
- 访问 `http://localhost:3002/` 无页面的主要原因：`index.html` 没有加载应用入口脚本，当前只有挂载点 `#root`，未引入 `index.tsx`。
  - 证据：`index.html:41-43` 仅有 `<div id="root"></div>`，缺少 `<script type="module" src="/index.tsx"></script>`。
- 次要影响因素：`importmap` 引用的 CDN 包在 Vite 场景下不是必需，但保留不会阻止页面加载；关键仍是入口脚本缺失。
- 运行要求：前端 Dev Server 与后端 API 同时运行，前端通过代理 `/api` → `http://localhost:4000`。

## 实施步骤
1. 在 `index.html` 的 `</body>` 之前添加入口脚本：
   - 新增一行：`<script type="module" src="/index.tsx"></script>`。
2. 启动后端服务（若未运行）：
   - `cd server && npm install`
   - 将 `server/.env.example` 复制为 `.env` 并设置 `GEMINI_API_KEY`
   - `npm run dev`（监听 `4000`）
3. 启动前端服务：
   - 在项目根执行：`npm install --legacy-peer-deps`
   - `npm run dev`（自动选择端口，例如 `3002`）
4. 保留现有 `vite.config.ts` 代理配置，确保 `/api` 请求正常转发到后端。

## 验证方案
- 页面渲染：打开 `http://localhost:3002/`，应展示 Onboarding 页面（学段选择）。
- 代理联通：访问 `http://localhost:3002/api/health` 返回 `{"ok":true}`。
- 端到端：设置有效 `GEMINI_API_KEY` 后，选择学段，能够拉取词汇并进行造句评估；若 500，则检查密钥或网络。
- 浏览器控制台：无红色报错（若有与 importmap 相关的网络错误，可忽略，只要入口脚本加载与前端编译包生效即可）。

## 可选优化（不影响页面加载）
- 移除 `index.html` 的 `importmap` 与前端 `package.json` 中的 `@google/genai` 依赖，避免误用与体积膨胀；Vite 将从本地依赖打包 React/Lucide。
- 为后端增加日志与错误细节返回，便于定位生成失败的具体原因（速率限制、网络异常等）。

请确认以上计划，我将立即按步骤修改 `index.html` 并重启/验证整个环境。