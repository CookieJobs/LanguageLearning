# 答题页面布局修改检查清单 (Checklist)

## 开发前检查
- [x] 确认需求：将答题页面从左右结构改为上下结构。
- [x] 定位文件：确定目标组件为 `LearningSession.tsx`。

## 代码修改检查
- [x] 确认主容器已经移除了 `md:flex-row` 属性，强制使用 `flex-col`。
- [x] 确认原左侧栏现已改为顶部区域，去除了 `md:w-[38%]` 等宽度限制。
- [x] 确认顶部区域边框从右边框（`border-r-4`）成功修改为底边框（`border-b-4`）。
- [x] 确认底部区域配置了正确的 flex 属性和 `overflow-hidden`，保证其子元素可以正常滚动。

## 测试与验证
- [x] 确认代码可以正常编译和热更新。
- [ ] 视窗测试：在不同屏幕宽度下（移动端、平板、桌面端）预览答题页面，确认页面始终呈现上下结构。
- [ ] 功能测试：进行一次选择题和造句题测试，确保答题区域内的所有功能（输入、点击、滚动、下一题按钮）依然可用且未被遮挡。
- [ ] 提示测试：确认造句题中的提示框（Hint Dropdown）展开时不会影响整体页面布局或引发意料之外的滚动条行为。
